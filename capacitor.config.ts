import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hito2grupal2t.app',
  appName: 'hito_grupal_carlos_celia',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
